
document.addEventListener("DOMContentLoaded", () => {
    const bounceText = document.getElementById("bounce-text");

    bounceText.style.animationPlayState = "running"; // Start the animation immediately
});
